- [x] mark the date on the graph — trade points now carry timestamps; day-boundary ticks + MM-DD labels on the chart, time in point tooltips
- [x] mark the update to the algorithm on the graph — algo_update/start/stop/restart events drawn as colored dashed vertical markers (legacy `reloading` events count as algo updates)
- [x] add event on algo updates — daemon writes `algo_update` (with code mtime) instead of noise-kind `reloading` when a deploy changes code on disk; shows in event feed + Telegram digest
- [x] add events for start/stop/restart of bot — `bot_start` / `bot_stop` / `bot_restart` written when Pause/Resume/Reload commands are consumed; feed + chart + Telegram
- [x] make sure telegram push dont over send — prod events show no double-sends: distinct grid cycles at the same level produce byte-identical messages (same prices/qty/P&L). Every Telegram line now ends with a unique [#event-id] tag so real repeats are distinguishable from double-sends. If a tagged id ever arrives twice, that's a transport retry — report it.
- [x] think outside the box, analyse historicals (real BTCUSDT), propose a new path to profit — see docs/proposal-two-sided-grid.md. Short-grid idea proposed AND same-day refuted by the 2024 cross-regime test (routed version loses on both tapes; lagging routers put inventory on the wrong side of reversions). What survives every test: long grid only in strongly favorable regimes + hard flat otherwise (already implemented), and a 3-gate acceptance protocol (both-tapes walk-forward, 4wk paper soak vs flat, USDT+buy-hold benchmarks) so future ideas can't be tape-of-the-month. Funding carry refuted for BTC this year (~2%/yr).
